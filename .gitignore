# Third-party: NirSoft MultiMonitorTool, redistributed under its own terms.
MultiMonitorTool.exe
MultiMonitorTool.chm
MultiMonitorTool.cfg
readme.txt
files.zip

# Build outputs - sources are tracked, binaries are not.
bin/SetWallpaper.exe
bin/DisplayCtl.exe

# Local snapshots taken while debugging.
_backup-predell/
*.orig-backup
*.midchaos-backup
mode.state

# Written at runtime: machine state, not source.
*.log

# Local snapshots taken before overwriting a saved layout.
*.bak
